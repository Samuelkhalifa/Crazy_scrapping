require_relative "../lib/crazy_scrapping"

describe "the import_crypto_names method" do
    it "should return an array of crypto names" do
    expect(array_names).to eq()
    end
end

describe "the import_crypto_values method" do
    it "should return an array of crypto values" do
    expect(array_values).to eq()
    end
end